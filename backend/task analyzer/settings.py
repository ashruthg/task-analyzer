# Placeholder Django settings to indicate structure. Not a real settings file.
SECRET_KEY = 'placeholder'
INSTALLED_APPS = ['tasks']
DATABASES = {'default': {'ENGINE': 'django.db.backends.sqlite3', 'NAME': 'db.sqlite3'}}